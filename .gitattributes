wheels.zip filter=lfs diff=lfs merge=lfs -text
# Only the TensorFlow wheel exceeds GitHub's 100MB per-file limit; the rest stay regular git objects.
tensorflow-*.whl filter=lfs diff=lfs merge=lfs -text
